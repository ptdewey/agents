# Data directories (persisted between restarts)
prometheus_data/
tempo_data/
grafana_data/

# Optional: don't persist metrics if you want a fresh start
# Uncomment the line below if you want to reset metrics on restart
# prometheus_data/